name: Test Action

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: echo "Works"
      - runs: echo "${{ vars.MY_VAR }}"
      - runs: echo "${{ secrets.MY_SECRET }}"
